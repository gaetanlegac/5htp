export const h1 = (titre: string) => console.log(
    '\n' +
    '######################################################\n' +
    '# ' + titre.toUpperCase() + '\n' +
    '######################################################'
);

export const h2 = (titre: string) => console.log(
    '------------------------------------------------------\n' +
    '> ' + titre.toUpperCase() + '\n' +
    '------------------------------------------------------'
);